{
  "name": "@kafeasist/redis",
  "version": "0.1.0",
  "main": "./index.ts",
  "types": "./index.ts",
  "scripts": {
    "build": "pnpm with-env tsc --build",
    "watch": "pnpm with-env tsc --watch",
    "typecheck": "pnpm with-env tsc --noEmit",
    "with-env": "dotenv -e ../../.env --"
  },
  "dependencies": {
    "@kafeasist/typescript": "workspace:*",
    "@upstash/ratelimit": "^1.2.1",
    "@upstash/redis": "^1.29.0",
    "dotenv-cli": "^7.4.1",
    "typescript": "catalog:"
  },
  "private": true,
  "prettier": "@kafeasist/prettier"
}
